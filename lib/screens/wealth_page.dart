import 'package:flutter/material.dart';

class WealthPage extends StatelessWidget {
  static const String id = 'wealth_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wealth'),
      ),
      body: Text('Wealth Page'),
    );
  }
}
